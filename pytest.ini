[pytest]
# 현재 디렉터리를 모듈 탐색 경로(PYTHONPATH)에 자동으로 추가
pythonpath = .

# 기본 실행 옵션 설정
# -v: 상세 출력
# -p no:capture: TUI 래퍼 충돌 방지를 위해 표준 출력 캡처 비활성화
# --strict-markers: 등록되지 않은 마커 사용 시 경고
addopts = -v -p no:capture

# 테스트 파일 탐색 패턴
python_files = test_*.py
testpaths = tests
